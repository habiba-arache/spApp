## Hibernate Configuration

The file `hibernate.cfg.xml` is not included in this repository for security reasons (it contains database credentials).  
However, it **was used in the project**, and it is included in the version I submitted directly to the professor.

The configuration defines:
- Database connection (URL, username, password)
- Mapped entities
- hbm2ddl settings
